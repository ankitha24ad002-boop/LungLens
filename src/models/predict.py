# src/models/predict.py (Dummy version for Person 4 to use immediately)
import numpy as np

def predict_image(image_path):
    # TODO: Person 2 will replace this with the real PyTorch model later
    print(f"Fake predicting for: {image_path}")
    return {"Normal": 0.1, "Pneumonia": 0.8, "Tuberculosis": 0.1}
